# 오픈소스SW개론 01분반 2차 과제
**GITHUB에 README파일로 과제 수행하기.**

**이름: 황다민**  
**학번: 20213092**  
**학과: 컴퓨터공학과**

### 1. 리눅스 명령어 (top, ps, jobs, kill)
#### 1) **top 명령어**  
  **top** 명령어는 **현재** OS상태를 나타내어주는 **CLI 어플리케이션임!**  
    - CPU, RAM, PROCESS등 시스템의 상태를 빠르게 알아볼 수 있음.  
    - **TOP**명령어   

        Shift + p : CPU사용율 내림차순 정렬  
        Shift + m : 메모리 사용율 내림차순 정렬  
        Shift + t : 프로세스가 돌아가는 시간순서로 정렬  
        k         : kill, k 입력후 PID번호 작성. signal은 9임  
        f         : sort field 선택 화면 -> q 누르면 RES순으로 정렬  
        a         : 메모리 사용량에 따라 정렬  
        b         : Batch 모드로 작동함  
        1         : CPU 코어별로 사용량을 보여줌  
        (PID: 프로세스 ID...?)  
        (RES: 누수 체크가 가능한 메모리 메모리의 사용량을 보여줌.
        또한, RES는 현재 프로세서가 사용하고 있는 물리 메모리의 양을 표시해주므로 중요함!)

#### 2) **ps 명령어**
  **ps** 명령어는 위의 **top** 명령어와 매우 비슷함!
    - 그렇다면 왜 ps명령어가 따로 있는가?  
      top는 **현재**의 시각에 따른 사용률을 보여준다고 가정을 한다면, ps는 **현재가 아닌 프로세스 전체 사용시간의 사용률**을 보여준다!  
    - **PS**명령어  
    
         ps -e   : 커널 프로세서를 제외한 모든 프로세스 출력  
         ps -f   : 포멧 형태로 출력(풀 형태)  
         ps -l   : 포멧 형태로 출력(긴 형태)  
         ps -A   : 모든 프로세서 출력  
         ps -a   : 세션리더를 제외하고 종속되지 않은 모든 프로세서 출력  
         ps  a   : 터미널과 관련된 프로세서를 출력하는 옵션  
         ps -p   : 특정 PID를 지정할 때 사용되는 옵션  
        
#### 3) **jobs 명령어**
  **jobs** 명령어는 작업의 상태를 표시하는 명령어로, 쉘 세션에서 실행하고 있는 백그라운드의 작업 목록을 확인할 수 있다!  
  각 작업에는 번호가 붙어있어 kill명령어를 사용하기에 더 간편하다!  
    - **jobs**명령어     
         
         -l      : 프로세서 그룹 ID를 state 필드 앞에 출력
         -n      : 프로세서 그룹 중에 대표 프로세스 ID를 출력
         -p      : 각 프로세서 ID에 대해 한 행씩 출력
         command : 지정한 명령어를 실행
         
#### 4) **kill 명령어**
  **kill** 명령어는 리눅스 작업중 프로세스가 작동을 하지 않을때, 종료를 하기 위한 명령어이다!  
   안전하게 종료(방법은 SIGTERM 시그널을 보냄. 기본적인 동작 자체가 프로그램 종료)를 진행해줌!**절때 강제 종료가 아님!**  
   강제 종료하는 방법은 SIGKILL 시그널을 보내면 됨. (권장X)  
    - 사용 방법  
        **kill Options PID**  
        Options에는 명령어 번호를 ex) -9, -15  
        아니면 이름을 ex) Sigkill, Sigterm 사용해도 상관이 없다.  
    - **kill**명령어  
    
           kill 명령어는 매우 다양하고 종류가 많다... 주로 사용하는 2개를 가져온다면.....
           Signal 종류 9번, 15번을 자주 사용한다.
           9 ) Sigkill : 죽이기 (강제 종료라고 생각하면 됨.)
           15) Sigterm : 소프트웨어 종료 시그널
    

### 2. VIM에디터에서 매크로 사용방법(q, @)
  #### VIM 에디터에서 매크로는 q를 눌러서 사용한다!
  - **매크로 시작할 때?**  
    q[NAME] NAME에 자신이 지정할 키를 입력해주면, 바로 매크로가 동작하기 시작한다. ex) qq, qt, qa  
    바로 자신이 수행할 키를 입력해주면 알아서 저장된다!  
  - **매크로를 지정할 때?**  
    q를 다시 눌러서 종료를 해준다.  
  - **매크로를 실행할 때?**  
    @[NAME] 을 해주면 아까 내가 지정했던 NAME매크로를 실행 할 수 있다.
  - **응용버전!**  
    [NUMBER]@[NAME] 을 해주면, 지정한 NUMBER만큼 NAME매크로가 시행된다!  
  - **직접하기 복잡하다면?**
    10개의 a를 입력해줄 때, insert를 누르고........   직접 하기 힘든 작업이 있다고 한다면 매크로를 사용하자!  
    상당히 비효율적인 작업을 진행 할 때, 매크로를 이용한 작업을 수행해준다면, 빠르고 더 적게 누를 수 있다.      
  - **VIMGOLF를 직접 수행할 때....  매크로를 사용했던 경험**  
    1차 과제였던 vimgolf를 수행하면서 키 스트로크를 줄일 때 활용한 매크로를 활용한 경험이 있다. 직접 누를땐, 20개 이상이던 것이 10개로 줄여지는 것으로 보아,  
    짧은 시간에 단축할 수 있던 부분이 좋았던 것 같다.  

### 참고했던 블로그 정보.
[Top명령어_참고_블로그1](https://sabarada.tistory.com/146)  
[Top명령어_참고_블로그2](https://zzsza.github.io/development/2018/07/18/linux-top/)  
[Ps명령어_참고_블로그1](https://jhnyang.tistory.com/268)  
[Ps명령어_참고_블로그2](https://kldp.org/node/153790)  
[Jobs명령어_참고_블로그](https://hbase.tistory.com/265)  
[kill_명령어_참고_블로그](https://sisiblog.tistory.com/209)  
